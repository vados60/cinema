<%= form_for(@hall) do |f| %>
  <% if @hall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hall.errors.count, "error") %> prohibited this hall from being saved:</h2>

      <ul>
      <% @hall.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :nor %><br />
    <%= f.number_field :nor %>
  </div>
  <div class="field">
    <%= f.label :nos %><br />
    <%= f.number_field :nos %>
  </div>
  <div class="field">
    <%= f.label :cinema_id %><br />
    <%= f.text_field :cinema_id, :value => @cinema_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
